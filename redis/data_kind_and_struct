1、redis的数据类型（string,list,hash,set,zset）
string:动态字符串类型
list：压缩列表和双向链表
hash：压缩列表和hash表 （压缩列表可以结构：【列表长度】【列表尾的偏移量】【列表entry个数】【entries】【表结尾】，压缩列表可以快速定位首尾元素O(1),其它O(n)）
set：hash表和整数数组  （hash表表示处理唯一性，整数数组存储元素）
zset：hash表和跳跃表和压缩列表  （hash表处理唯一性，跳跃表保证集合有序性和快速查找）

2、redis的渐进式扩容
发生扩容时，会开辟一个当前内存大小2倍的空间，再此期间的写操作，会全部落入到新的内存中，每当发起一次对就空间的读写时，都会将就空间的第一个entry进行搬迁
。此外，还会启动一个定时任务（100ms），操作上诉步骤，防止长时间没读取任务导致rehash时间过长